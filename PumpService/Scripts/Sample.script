namespace Sample
{
	public class SampleScript
	{

		public bool EntryPoint()
		{
			System.Random random = new System.Random();
			int number = random.Next(1,100);
			return number > 10 || number < 40 ? true : false;
		}

	}
}